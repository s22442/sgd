#ifndef CONFIG_H
#define CONFIG_H

const auto WINDOW_WIDTH = 800; // px

const auto WINDOW_HEIGHT = 600; // px

const auto BALL_SPEED = 8;

const auto INITIAL_BALL_SPEED = 3;

const auto PLATFORM_SPEED = 12;

const auto WALL_EXTENSION = 10; // px

const auto BRICK_GRID_MARGIN_TOP = 60; // px

const auto BRICK_COLS = 9; // px

const auto BRICK_ROWS = 6; // px

const auto BRICK_CELL_WIDTH = 76; // px

const auto BRICK_CELL_HEIGHT = 32; // px

#endif // CONFIG_H
