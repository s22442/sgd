#ifndef CONFIG_H
#define CONFIG_H

const auto WINDOW_WIDTH = 800;

const auto WINDOW_HEIGHT = 600;

// todo @kw balance speed values
const auto BALL_SPEED = 5;

const auto PLATFORM_SPEED = 8;

#endif // CONFIG_H
